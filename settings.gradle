rootProject.name = 'Familiada'
include 'Model'
include 'Infrastructure'

if (vaadinFrontend.toBoolean()) {
    include 'ClientVaadin'
}
else if (reactFrontend.toBoolean()) {
    include 'ClientReact'
}

